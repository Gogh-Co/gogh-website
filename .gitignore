# Nuxt dev/build outputs
.output
.output/
.data
.nuxt
.nuxt/
.nitro/
.cache/
.agents/
.claude/

dist/
# `dist/` above only matches a real directory - wrangler pages tooling
# sometimes creates `dist` as a symlink instead (e.g. -> .output/public),
# which that pattern doesn't catch.
/dist

# Auto-generated by @nuxt/test-utils on first run; local tooling state, not
# project config.
.nuxtrc

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
bun.lock
